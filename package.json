{
  "name": "calendoo",
  "packageManager": "yarn@3.5.1",
  "workspaces": [
    "apps/*"
  ],
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "dotenv-cli": "^7.2.1",
    "turbo": "^1.10.12",
    "typescript": "^5.0.4"
  },
  "engines": {
    "node": ">=18.16.0",
    "yarn": ">=3.5.1"
  }
}
